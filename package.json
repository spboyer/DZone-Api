{
  "name": "dzone-api",
  "description": "REST API for RSS Feed aggregation and normalization from DZone.com",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "3.x"
  }
}
